name: Prueba programada automática

on:
  schedule:
    - cron: "7,17,27,37,47,57 * * * *"

jobs:
  prueba:
    runs-on: ubuntu-latest

    steps:
      - name: Confirmar ejecución automática
        run: |
          echo "El evento schedule funciona correctamente"
          date -u
